language: python
python: 2.7

env:
  - &base
    HELLO: "hello"
  - *base
  - HELLO: "HELLO"
    WORLD: "WORLD"
  
matrix:
  include: &hello
    - HELLO: "hello"
  allow_failures:
    <<: *hello
    - WORLD: "WORLD"
 
build:
  ci:
    - echo ${HELLO} ${WORLD}


